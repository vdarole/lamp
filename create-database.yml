---
   - name: Create database on server.
     hosts: db
     become: true
     become_user: root
     tasks:
     - name: Create a new database 
       mysql_db: 
         name='{{ dbname }}' 
         state=present 
         login_user=root 
         login_password=redhat
...
